// place files you want to import through the `$lib` alias in this folder.
import { something } from '@pdf-tools/domain';
import { somethingElse } from '@pdf-tools/pdf-engine';
import { util } from '@pdf-tools/shared';

console.log(something, somethingElse, util);
