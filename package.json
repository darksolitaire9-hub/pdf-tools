{
  "name": "pdf-tools",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "type-check": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "prettier": "^3.7.4",
    "eslint": "^9.39.1",
    "vitest": "^4.0.15"
  }
}
